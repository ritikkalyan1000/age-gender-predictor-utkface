# age-gender-predictor-utkface
Multi-output CNN model that predicts both age (regression) and gender (classification) from facial images using the UTKFace dataset. Built with TensorFlow/Keras, optimized with dual-loss, and trained on real-world Kaggle data.
